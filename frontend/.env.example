# Public URL only: never put secrets in VITE_* variables.
VITE_API_BASE_URL=http://localhost:8000/api
# Keep local development at the root. GitHub Actions overrides this with /teamarcher/.
VITE_BASE_PATH=/
